// Escribe tu código aquí.

function stringWords (word, array)  {
    "Introduce un string"; 

const myArray = ['insecto', 'bootcamp', 'mangos', 'reptil', 'mosca', 'escritorio', 'hola','goma','sacapuntas','limon'];
 
stringWords('bocina', myArray);('filterButton')


document.getElementById("filterButton").addEventListener('click', function() {
        const inputWord = document.getElementById('inputWord').value;
        onst myArray = ['insecto', 'bootcamp', 'mangos', 'reptil', 'mosca', 'escritorio'];

           
const filteredWords = stringWords(word, array);

printArray(filteredWords);  });
  
</body>

document.getElementById


console.log(filteredWords);